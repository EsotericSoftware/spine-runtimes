name: spine_test
description: A simple CLI app to test spine_flutter library

environment:
  sdk: ^3.0.0

dependencies:
  ffi: ^2.0.0
  spine_flutter: ^4.3.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0

dependency_overrides:
  spine_flutter:
    path: ../